import { EliminarDuplicadoPipe } from './eliminar-duplicado.pipe';

describe('EliminarDuplicadoPipe', () => {
  it('create an instance', () => {
    const pipe = new EliminarDuplicadoPipe();
    expect(pipe).toBeTruthy();
  });
});
